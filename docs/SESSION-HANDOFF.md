# Session Handoff: #68 project sub-entities SHIPPED & CLOSED (PR #73) → next is #70 config-writes (LAST of epic #51, still plan-review)

Last updated: 2026-06-10. Repo: `main` @ `ab51d5c` (Zod 4.4 / TypeScript 6 / Vitest 4), single worktree `~/repos/pipedrive-mcp-server` (plus the `#70` agent worktree from the plan stage), clean. Build green, **1490 tests green**, CI green Node 20/22.

This session ran the full menehune pipeline on **#68** (`/backlog:review-plan 68` ×3 → `/backlog:implement 68` → `monitor` → `merge`): two `ce-doc-review` rounds (feasibility clean both times) → approved as-written, implemented all 7 units, CI passed, squash-merged via PR **#73** → `main @ ab51d5c`, **#68 auto-closed** (`Closes #68`, `status:done`). The one remaining roadmap issue **#70** is the **LAST** of epic **#51** and is at **`status:plan-review`** (planned 2026-06-09, not yet reviewed). Epic **#51** closes when #70 lands (#67 + #68 + #69 now done).

## Done this session (do NOT redo)

- **#68 — Project sub-entities SHIPPED & CLOSED** via PR **#73** (squash-merged → `main @ ab51d5c`; commit "feat: project sub-entities — 21 tools, 8 endpoint groups (#68) (#73)"). **21 new v2 tools** across 7 units → **129 tools total, no duplicate names**. New files `src/{schemas,tools}/tasks.ts` + `src/{schemas,tools}/boards.ts`; edits to `src/{schemas,tools}/projects.ts`, `src/{schemas,tools}/fields.ts`, and **`src/tools/index.ts`** (spreads `taskTools`/`boardTools`/`phaseTools`). Branch was **rebased onto current `main` first** (it was cut from old main @ `420b416` pre-#67/#69; the touched files index/projects/fields were unchanged on main since the branch point, so the rebase was clean). **1490 tests green** (+227), tsc + `eslint src/` clean.
  - **U1+U2 — Tasks CRUD** (×5): `pipedrive_{list,get,create,update,delete}_task` over `/tasks`. `done`/`milestone` are **int `0|1`** in the write body (GET returns `is_done`/`is_milestone` **bool** — response-only) — implemented with **`z.preprocess` coercing `true`→1/`false`→0** before `z.union([z.literal(0),z.literal(1)])`, plus the 0/1 rule stated in the **LLM-visible tool descriptions**. `parent_task_id` is **`z.string().optional()`** (allows the `"null"` root-only sentinel). Delete gated by `destructiveOperationGuard()`, summary notes the subtask cascade.
  - **U3 — Boards + Phases CRUD** (×5 + ×5): `pipedrive_{list,get,create,update,delete}_{board,phase}` over `/boards`, `/phases`. **No cursor pagination** — list handlers do NOT call `buildPaginationParamsV2`/`extractPaginationV2` and omit the `pagination` key. **`/phases` list REQUIRES `board_id`** (`z.number().int().positive()`, not optional). Deletes gated.
  - **U4 — Project templates** (×2): `pipedrive_{list,get}_project_template` over `/projectTemplates` (read-only). **NOT beta-tagged** (Q5: the spec doesn't tag `/projectTemplates` Beta) — the two template descriptions omit the beta disclaimer; all other new project tools include it.
  - **U5 — Project fields** (×1): `pipedrive_list_project_fields` over `/projectFields` (mirrors `listProductFields`).
  - **U6 — Archived projects** (×1): `pipedrive_list_archived_projects` over `/projects/archived` with `filter_id`/`status`/`phase_id`.
  - **U7 — Permitted users + changelog** (×2): `pipedrive_get_project_permitted_users` over `/projects/{id}/permittedUsers` (response `data` is a flat **`integer[]`** of user IDs — typed `client.get<number[]>`, bespoke summary, no `extractPaginationV2`); `pipedrive_get_project_changelog` over `/projects/{id}/changelog` (cursor paginated).
  - Also **sharpened the existing `pipedrive_list_project_tasks` description** to disambiguate from the new general `pipedrive_list_tasks` — it says pass `id` (the project ID), **NOT `project_id`** (the input field is `id`; `project_id` is only the internal query key).
  - **Implement method:** firewalled pipeline — 3 sequential write-capable sonnet implementers by file-domain (tasks / boards+phases / projects-ext+fields), then independent opus `code-reviewer` = **APPROVE, no P0/P1/P2** (2 P3 cosmetic only), then **adversarial revert-proof** (full `src/` revert → 8 files / 134+ guards fail, zero false-greens). CI green Node 20/22.
  - **Plan review:** 2 `ce-doc-review` rounds. R1 fixed 8 findings (traceability table off-by-one, phantom U8 removed, Q4 resolved, R-1 redundancy decision, R-4 coercion, R-8/R-9 notes, sequencing). R2 caught 3 more — 2 of them artifacts the R1 revision itself introduced (PR-grouping drift + a wrong tool-description param name). Then approve-as-written.

### Prior sessions (still valid — do NOT redo)

- **#67 — Deal sub-resources SHIPPED & CLOSED** via PR **#72** (`main` ancestor `f89aca0`). 16 v2 tools across 5 units on `src/{schemas,tools}/deals.ts`, no `index.ts` change. Notables: discounts `discount_id` is a UUID (`z.uuid()`), list has no pagination; installments list is **collection-level** `/deals/installments` with required comma-joined `deal_ids`; convert-to-lead is GATED (marks source deal deleted) + empty `{}` POST + status-poll. **Open follow-up:** one-time manual smoke of the installments path vs a Growth+ sandbox (CI mocks only).
- **#69 — Cross-entity followers + multipart media SHIPPED & CLOSED** via PR **#71** (5 units, 15 tools): deal/person/org follower CRUD+changelog (1:1 of the product-follower pattern; **#69 owns deal followers** — #67 excluded them); `pipedrive_get_person_picture` (read-only); `postMultipart`/`putMultipart` on the client sharing private `buildRequestUrl`/`applyAuth`/`parseResponse`/`networkError`; product-image upload (POST) + update (PUT) with hybrid `file_path`|`base64_data`.

## Next action

**#70 is the LAST issue — run `/backlog:review-plan 70`, then `/backlog:implement 70`.** Its plan is committed to `agent/70-config-writes` (worktree at `.claude/menehune/worktrees/agent-70-config-writes`, planned 2026-06-09 — **the worktree is on OLD main; rebase onto current `main` @ `ab51d5c` before/at implement**, same as #68 did). Re-verify EVERY endpoint/param/response against `docs/api/openapi-v2.yaml` at implement time (roadmap/plan prose can carry shape guesses — the #50 U6 drift was the cautionary tale). Re-run `gh issue list --state open` to confirm the board first.

- **#70 — Config writes** (files `fields.ts`/`pipelines.ts`/stages, `area: migration`): pipelines/stages/fields CRUD with v2 renames; **+ deferred product-field write ops** (`add/update/deleteProductField` + options). Spec catches: **`is_deleted` is READ-ONLY** (must NOT appear in any write body — deletion is via DELETE only; the `active→is_deleted` rename does NOT add a write field); `selected→is_selected` likewise doesn't appear in any in-scope write body; stage renames `rotten_flag→is_deal_rot_enabled`/`rotten_days→days_to_rotten` + pipeline `deal_probability→is_deal_probability_enabled` (note a SEPARATE un-renamed stage-level integer `deal_probability` also exists). All deletes gated. Plan recommends 2 PRs: U1+U2 (pipeline/stage writes) then U3+U4 (field writes). Memory flagged it for a 5-persona `ce-doc-review` given its rename-correctness risk.
- **#70 conflict note:** #70 edits `fields.ts`, which #68 also touched (added `listProjectFields`). Both are now on main, so #70's worktree must **rebase onto `ab51d5c`** to pick up the #68 `fields.ts` changes before editing it.

## Pipeline + gates (per memory backlog-orchestration-workflow)

The menehune `/backlog:*` commands ARE available this session (assess→plan→review-plan→implement→monitor→merge→close). Per issue: plan(opus, writes+commits plan to `docs/plans/`, posts to issue, → `status:plan-review`) → review-plan (optionally runs `ce-doc-review`; approve → `status:implement`) → implement (off the approved plan; write units + incremental commits + `npm run build`/`npm test`; **firewalled subagent pipeline preferred over `ce-work` for worktree correctness** — write-capable implementer(s) by file-domain → independent `code-reviewer` → adversarial revert-proof) → denylist → PR `Closes #N` → monitor CI → squash-merge → close. Per-issue worktree `.claude/menehune/worktrees/agent-N-slug` off `origin/main`, branch `agent/N-slug`, lock `.claude/menehune/locks/N.lock`; **symlink `node_modules` into the worktree** (no `npm ci`) when no deps change. Denylist before push: `.env*`, `package-lock.json`, `bundle/`, `*.mcpb`, `node_modules`. **#70 worktree + branch already exist** from the plan stage — implement reuses them (rebase first).

## Known gotchas (carried)

- **MERGE-GATE GOTCHA — DODGED cleanly this session.** Instead of `gh pr merge <n> --squash --delete-branch` (which has failed its local-branch delete + skipped the remote delete 5× because the **worktree** holds the branch), use **`gh pr merge <n> --squash` WITHOUT `--delete-branch`**, then manual cleanup from the MAIN repo root in order: `git worktree remove <path> --force` → `git branch -D <branch>` → `git push origin --delete <branch>` → `git worktree prune` → `rm -r .claude/menehune/locks/N.lock` → `git checkout main && git pull --ff-only`. This produced zero half-failed state (#68 and #50-slice3 both clean this way). Verify the merge with `gh pr view <n> --json state,mergedAt` (don't trust exit codes), worktree gone with `git worktree list`, remote branch gone with `git ls-remote --heads origin <branch>`.
- **Verify LLM-facing tool-description text against the actual Zod INPUT schema, not the API query key.** #68 R2 caught a sharpened `listProjectTasks` description that told the model to pass `project_id` — but the tool's input field is `id` (`ListProjectTasksSchema.extend({ id })`; the handler maps `params.id` → the `project_id` query key). A description prescribing the wrong param name would actively mislead tool selection.
- **`/backlog:implement` ships the WHOLE issue as ONE PR.** #68's plan recommended a multi-PR split (PR A=U4+U6, PR B=U7, U5 separate); all 7 units landed in PR #73. If a genuine slice strategy is needed, drive slices manually rather than via one implement invocation.
- **`git checkout <ref> -- src/` does NOT delete files absent from `<ref>`** — for a complete revert-proof you must `rm` the NEW files (e.g. `tasks.ts`/`boards.ts`) explicitly, then `git checkout origin/main -- <modified existing files>`, run tests (expect failures), then `git checkout HEAD -- src/` to restore. (Reverting only the modified-existing files leaves new-file handlers live, so their guards don't fire — a partial, falsely-reassuring revert-proof.)
- **Multipart real-boundary testing:** the `undici` package is NOT importable and the client base URLs are hardcoded, so `undici` `MockAgent` doesn't work. Run the exact `fetch` `init` through `new Request(url, init)` and assert the outbound `Content-Type` starts with `multipart/form-data; boundary=`.
- **Spec-vs-plan shape drift:** roadmap/plan prose contains endpoint/shape guesses. Always re-verify against `docs/api/openapi-v2.yaml` at plan + implement time.
- **Auth reality (supersedes CLAUDE.md prose):** v2 uses the **`x-api-token` HEADER** (`src/client.ts`), NOT the `api_token` query param — that's v1-only. The shared `applyAuth` helper is the single source of truth.
- **Lint IS a CI gate** (`.github/workflows/ci.yml` runs `npm run lint` = `eslint src/` — test files NOT linted in CI). Verify the real gate with `npx eslint src/`.
- **Adding a param means editing THREE places:** the Zod schema, the hand-written `inputSchema` JSON literal (NOT generated from Zod), and the handler's query/body/path forward line.
- **Zod 4.4.3 on main** (NOT Zod 3 — stale plan bodies may say "Zod 3.25"). Idioms: `z.email()`/`z.uuid()`, `z.record(z.string(), z.unknown())`, `.extend(...shape)` (not `.merge`), `error.issues` (not `.errors`).
- **`createListSummary` naively appends "s"** for plurals (so "changelog entry" → "changelog entrys", "1 task" → "Found 1 task."). It's the shared helper used everywhere — assert summaries with `toContain`, not exact `toBe`; the grammar quirk is pre-existing, not a regression.
- **False-green hazard:** `tsconfig` excludes `**/*.test.ts` (tests never type-checked) and per-entity tests call handlers DIRECTLY (bypass the dispatcher's Zod). Real guards = UNIT schema tests (`Schema.parse`) + integration tests asserting the wire shape (`JSON.parse(options.body)` / the URL / `FormData` body) + the `tests/contract/` OpenAPI harness.
- **Unit schema tests live in `tests/unit/schemas/`** (3-level import `../../../src/...`), integration handler tests in `tests/integration/tools/`. Test helpers (`mockFetch.ts`, `mockEnv.ts`) live at **`tests/helpers/`**. **`setupValidEnv()` sets `PIPEDRIVE_ENABLE_DESTRUCTIVE='true'` in `beforeEach`** — destructive "enabled" tests work by default; "guard blocks" tests must `delete process.env.PIPEDRIVE_ENABLE_DESTRUCTIVE` explicitly.

## Deferred / context

- **Deferred product-field WRITES (folded into #70):** `add/update/deleteProductField` + options. Only the field LIST read shipped (in #50 slice 3). This is the last deferred item and rides #70.
- **Wire Products into the contract harness (deferred P2, still open):** Products is NOT in `tests/contract/`. When next touching products, add `getProducts`/`searchProducts` to `requestParams.contract.test.ts` and `addProduct`/`updateProduct` to `requestBody.contract.test.ts`. (Consider the same for the new deal sub-resource + project/task handlers if a contract sweep is done.)
- **Tracker epic: #51** — Phases 0-3 done (#42-#50 + #60), Phase 3b: **#67 + #68 + #69 done**, **only #70 left in plan-review**; **#51 closes when #70 lands.**
